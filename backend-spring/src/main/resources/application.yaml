server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mingleup?serverTimezone=Asia/Seoul
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
  messages:
    encoding: UTF-8
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jpa:
    defer-datasource-initialization: true # Delay data.sql execution time to after DB initialization
    database-platform: org.hibernate.dialect.MySQLDialect # Use MYSQL dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY_ID}
      secret-key: ${AWS_SECRET_ACCESS_KEY}
    s3:
      bucket: mingleup-s3-bucket
    region: ap-northeast-2
    stack:
      auto: false

# This file should be used as gitignore in real work
jwt:
  secret: ${JWT_SECRET}
  expiration-in-ms: '360000000'

  # --- 카카오 로그인 설정 추가 ---
kakao:
  client-id: ${KAKAO_CLIENT_ID}
  client-secret: ${KAKAO_CLIENT_SECRET}
  # 배포용
  redirect-uri: '${BASE_URL}/api/v1/auth/kakao/callback'
  token-uri: "https://kauth.kakao.com/oauth/token"
  user-info-uri: "https://kapi.kakao.com/v2/user/me"
  auth-uri: "https://kauth.kakao.com/oauth/authorize"

app:
  frontend-base-url: ${FRONTEND_BASE_URL}